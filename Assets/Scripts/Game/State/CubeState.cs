namespace Game.State {
    public class CubeState {
        public string CubeId;
        public float Offset;

        public CubeState(string cubeId, float offset) {
            CubeId = cubeId;
            Offset = offset;
        }
    }
}